# Copy this file to .env and edit before running docker compose up

# Protects the "clear all photos" button in the app
ADMIN_PASSWORD=choose-a-real-password

# PostgreSQL credentials — used both to initialize the db container
# and to build the app's connection string
POSTGRES_USER=photowall
POSTGRES_PASSWORD=choose-a-real-db-password
POSTGRES_DB=photowall
